/* The [] in the var comentaries or similars, doesn't mean that the var
    is an array */

// Bishop type piece
function Bishop(identifier, color) {
    console.log('This is the bishop "' + identifier + '".');
}