/* The [] in the var comentaries or similars, doesn't mean that the var
    is an array */

// Horse type piece
function Horse(identifier, color) {
    console.log('This is the horse "' + identifier + '".');
}